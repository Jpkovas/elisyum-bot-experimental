# CHANGES

- Corrigi a validação de duração do YouTube para interpretar formatos como `HH:MM:SS`/`MM:SS` e usar fallback para `yt-dlp` quando os metadados rápidos não forem confiáveis, evitando bypass do limite de 6 minutos.
